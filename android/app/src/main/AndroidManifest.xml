<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- API 33+: scoped-storage gallery read for image picker. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!--
      API 34 PARTIAL grant marker. v1 treats a PARTIAL grant as a denial
      (Doc 3 §6 / §11 open item): we always request full READ_MEDIA_IMAGES
      and surface the "go to settings" banner on anything less. This line
      is declared so the system does not drop us onto the one-time-picker
      flow silently.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- API 29-32: legacy gallery read. Scoped out on 33+ via maxSdkVersion. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- API 33+: runtime notification permission for batch-complete notifications (Doc 3 §7). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Deliberately NOT declaring WRITE_EXTERNAL_STORAGE: MediaStore with
      RELATIVE_PATH + IS_PENDING does not require it on API 29+ (Doc 3 §6).
    -->

    <!--
      API 30+ package visibility: we must be able to resolve com.tencent.mm
      to share .jpg via an explicit WeChat intent (Doc 3 §5).
    -->
    <queries>
        <package android:name="com.tencent.mm" />
    </queries>

    <application
        android:label="Liveback"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
